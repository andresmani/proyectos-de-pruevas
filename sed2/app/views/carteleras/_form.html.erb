<%= simple_form_for @cartelera, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :mensaje %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                carteleras_path, :class => 'btn' %>
  </div>
<% end %>
